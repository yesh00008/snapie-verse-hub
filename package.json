{
  "name": "snapie",
  "version": "1.0.0",
  "description": "Snapie - Secure messaging platform with React, Node.js, Socket.IO, Firebase Auth, MongoDB",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["whatsapp", "clone", "react", "nodejs", "socket.io", "firebase", "mongodb"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}